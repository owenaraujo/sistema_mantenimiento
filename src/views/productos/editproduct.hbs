
    {{>navegacion}}

<div class="container pt-4">

        <div class="row">
            <div class="col-md-6 mx-auto">
                <form action="/productos/editproduct/{{product.id}}" method="POST" class="card card-body" >
                    <div>
                        <h1 class="mb-4 text-center">Edición de Productos</h1>
                    </div>
                    
                    <div>
                        <input type="text"value="{{product.producto}}" name="producto" class="form-control mb-4" placeholder="producto">
                    </div>  
                        <div>
                            <input type="text" value="{{product.codificacion}}" name="codificacion" class="form-control mb-4" placeholder="codificacion">
                        </div>
                    <div>
                        <input type="text"  value="{{product.peso}}" name="peso" class="form-control mb-4" placeholder="peso">
                    </div>
                    <div>
                        <input type="text" value="{{product.precio_por_lote}}" name="precio_por_lote" class="form-control mb-4" placeholder="precio por lote">
                    </div>
                    <div>
                        <input type="text" value="{{product.precio_unitario}}" name="precio_unitario" class="form-control mb-4" placeholder="precio unitario">
                    </div>
                    <div>
                        <input type="text" value="{{product.cantidad}}" name="cantidad" class="form-control mb-4" placeholder="cantidad">
                    </div>
                   
                    
                    <div class="btn-group" role="group" aria-label="Ejemplo básico" _mstaria-label="254332">
                        <button class="btn btn-primary btn-lg" >Editar</button>
                    </div>
                    
                </form>
            </div>
        </div>
    </div>
<div class="container p-4"></div>
